import 'package:flutter/material.dart';

class Walk extends StatelessWidget {
  const Walk({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Pata"),);
  }
}