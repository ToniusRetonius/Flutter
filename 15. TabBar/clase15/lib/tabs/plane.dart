import 'package:flutter/material.dart';

class Plane extends StatelessWidget {
  const Plane({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Avión"),);
  }
}