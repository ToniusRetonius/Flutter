/// un modelo es básicamente una clase

// ignore_for_file: file_names, unnecessary_this, prefer_initializing_formals

class Gif {
  String name;
  String url;
  /// una manera de definir el constructor de inicialización rápida :
  Gif(this.name, this.url);
}
